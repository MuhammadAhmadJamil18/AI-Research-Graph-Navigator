# Core dependencies
# Note: Python 3.14+ may have compatibility issues with some packages (Pydantic V1, spaCy)
# The app will work but some warnings may appear. Consider using Python 3.11 or 3.12 for best compatibility.
streamlit>=1.28.0
plotly>=5.17.0
networkx>=3.2.1
pyvis>=0.3.2

# RAG and embeddings
sentence-transformers>=2.2.2
# Note: ChromaDB may need to be installed separately due to dependency conflicts
# Try: pip install chromadb (will install latest compatible version)
chromadb  # Let pip resolve best version
faiss-cpu>=1.7.4
langchain>=0.1.0
langchain-community>=0.0.10

# LLM
ollama>=0.1.0
transformers>=4.35.0
torch>=2.1.0
peft>=0.7.0
accelerate>=0.24.0
bitsandbytes>=0.41.0

# Data processing
arxiv>=2.1.0
pandas>=2.1.0
numpy>=1.24.0  # Note: Python 3.13+ requires numpy>=2.1.0 for langchain-community compatibility
beautifulsoup4>=4.12.0
requests>=2.31.0

# Evaluation
ragas>=0.1.0
datasets>=2.14.0
scikit-learn>=1.3.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Hybrid search and reranking
rank-bm25>=0.2.2
sentence-transformers[cross-encoder]>=2.2.2

# Graph analysis
python-louvain>=0.16

# NLP (optional, for improved entity extraction)
# Note: spaCy may not be compatible with Python 3.14+. The app will work without it.
spacy>=3.7.0; python_version < "3.14"

# Utilities
python-dotenv>=1.0.0
tqdm>=4.66.0
pyyaml>=6.0.1
pydantic-settings>=2.0.0  # Required for ChromaDB compatibility with Pydantic v2

# Development tools (optional, for code quality)
# Install with: pip install -r requirements-dev.txt
# Or individually: pip install black isort flake8 mypy pre-commit
black>=23.12.0
isort>=5.13.0
flake8>=7.0.0
mypy>=1.8.0
pre-commit>=3.6.0
